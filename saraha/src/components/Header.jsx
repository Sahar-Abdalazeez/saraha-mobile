import { View, StyleSheet } from 'react-native';

export const Header = ({ ri }) => {
    return (
        <View style={styles.container} >

        </View>
    )
}

const styles = StyleSheet.create({
    container: {
        display: 'flex',
        width: '100%',
        backgroundColor: "#10bbb3",
        height: 50,
    }
})
